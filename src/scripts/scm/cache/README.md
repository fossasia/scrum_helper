# SCM cache helpers

Provider-aware cache key helpers can live here once cache behavior is moved behind the SCM provider contract.

Cache helpers should not fetch provider data or render report output.
