# SCM normalizers

Normalizers will convert provider API responses into Scrum Helper's shared report activity shape.

They should not fetch data, read popup DOM, write storage, or render report HTML.
